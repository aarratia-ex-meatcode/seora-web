mutation AddProject(
  $user_id: ID!
  $name: String!
  $domain: String!
  $settings: JSON
) {
  addProject(
    user_id: $user_id
    name: $name
    domain: $domain
    settings: $settings
  ) {
    id
    user_id
    name
    domain
    settings
    created_at
  }
}
